apiVersion: 1

providers:
  - name: 'default'
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10  # 대시보드 파일 변경 사항을 주기적으로 체크하는 시간 (초)
    options:
      path: /var/lib/grafana/dashboards  # JSON 파일이 있는 위치